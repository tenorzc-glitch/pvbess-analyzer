# CloudBase 配置 - 复制此文件为 .env.local 并填入实际值

# CloudBase 环境 ID（云开发控制台 → 环境 → 环境 ID，形如 xxx-1a2b3c）
# 留空则应用以纯离线模式运行（数据仅存浏览器 localStorage）
VITE_CLOUDBASE_ENV_ID=

# 语言（'zh' | 'en'）
VITE_DEFAULT_LANGUAGE=zh

# 主题（'light' | 'dark'）
VITE_DEFAULT_THEME=light